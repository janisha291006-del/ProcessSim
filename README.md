# ProcessSim
A Flask-based web simulator for CPU process scheduling algorithms (FCFS, SJF, SRTF, Round Robin) — visualizes Gantt charts, computes waiting/turnaround/response times, and compares all algorithms to find the best performer for a given process set.
